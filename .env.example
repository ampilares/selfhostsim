# Docker Compose Environment Variables

# GHL Integration (service-to-service)
# - Configure `ghl/.env` with `SELFHOSTSIM_API_BASE_URL` and `SELFHOSTSIM_INTERNAL_SECRET`
# - Configure `api/.env` with `INTERNAL_SECRET` (must match the GHL secret)

# GHL Inbound SMS Sync (shared config)
# Used by `api/` (to call `ghl/`) and by `ghl/` (provider-specific requirements)
SELFHOSTSIM_INTERNAL_SECRET=
GHL_CONVERSATION_PROVIDER_ID=

# Optional convenience URLs (varies by deployment)
SELFHOSTSIM_API_BASE_URL=http://selfhostsim-api:3001/api/v1
SELFHOSTSIM_GHL_BASE_URL=http://selfhostsim-ghl:3002/
